import { ApiConfigs } from "./type"

export const apiConfig: ApiConfigs = {
  jsonrpc: {
    endpoint: "jsonrpc",
    url: "https://localhost:5174/jsonrpc", // check out the readme for alternative RPC Relay urls
  }
}